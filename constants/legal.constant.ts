// Terms metadata for the /terms page. The article text lives in the message
// catalogs (messages/{en,ko}.json → terms.articles) so it can be translated.

export const TERMS_META = {
  effectiveDate: "2026-07-01",
  version: "1.2",
};
